# MAX Mini-App Constraints

- Use the MAX bridge adapter as the source of host integration events.
- Validate host session payloads server-side before trusting user identity.
- Respect host theme and viewport values when rendering UI.
- Keep generated navigation compatible with embedded host controls.

