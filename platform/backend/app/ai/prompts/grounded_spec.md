# Grounded Spec Extraction

Build `GroundedSpec` strictly from:

1. retrieved project documentation;
2. bundled platform documentation;
3. current template revision;
4. the current user prompt;
5. explicit assumptions only where required.

Do not invent undocumented APIs, env vars, or platform behavior.

