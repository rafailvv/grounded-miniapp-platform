# AppIR Build

Convert the validated `GroundedSpec` into `AppIR`.

- preserve traceability;
- emit only schema-compliant JSON;
- keep bindings explicit;
- keep compile blocking questions in `open_questions`.

